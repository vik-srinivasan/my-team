{
  "name": "my-team",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit -p tsconfig.base.json"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
