import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer C with the agent-prompt editor,
 * optional-specialist toggles, and effort-override selector.
 */
export function WorkflowTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">workflow placeholder</div>;
}
