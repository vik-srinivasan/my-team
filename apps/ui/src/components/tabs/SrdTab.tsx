import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer B with react-markdown rendering of
 * `srd.md`.
 */
export function SrdTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">srd placeholder</div>;
}
