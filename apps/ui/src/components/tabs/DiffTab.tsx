import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer B with `react-diff-viewer-continued`
 * rendering the session diff (unified / side-by-side toggle).
 */
export function DiffTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">diff placeholder</div>;
}
