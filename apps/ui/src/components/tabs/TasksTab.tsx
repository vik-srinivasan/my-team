import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer B with a checkbox-styled view of
 * `tasks.md` matching `team tasks` output.
 */
export function TasksTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">tasks placeholder</div>;
}
