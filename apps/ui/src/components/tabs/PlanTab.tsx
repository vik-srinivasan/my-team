import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer B with react-markdown rendering of
 * `plan.md`.
 */
export function PlanTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">plan placeholder</div>;
}
