import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer C with an xterm.js wrapper piped
 * bidirectionally through `useSessionWebSocket`.
 */
export function TerminalTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">terminal placeholder</div>;
}
