import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer B with react-markdown rendering of
 * `review.md` (collapsible iteration sections).
 */
export function ReviewTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">review placeholder</div>;
}
