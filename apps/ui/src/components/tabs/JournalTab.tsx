import type { ReactElement } from 'react';

/**
 * Placeholder — replaced by Engineer B with a react-markdown view that
 * auto-scrolls on WS `team_file` updates for `journal.md`.
 */
export function JournalTab(): ReactElement {
  return <div className="p-4 text-sm text-neutral-500">journal placeholder</div>;
}
